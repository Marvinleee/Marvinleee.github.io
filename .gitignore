# Bundler 本地安装缓存
# ⚠️ Gemfile.lock 必须保持忽略（2026-09-13 / 2026-09-14 两次全站样式丢失的根因）：
#    本机是 Intel macOS，为绕开 sass-embedded 崩溃而 vendored 了 libc++ 头文件，
#    本地解析会把 jekyll-sass-converter 粘在 2.2.0（LibSass）。该锁一旦提交，
#    CI（ubuntu）会忠实照做 → LibSass 不支持主题的 @use 语法 → 主题 CSS
#    从 109,940 B 塌缩成 ~1.2 KB，全站样式丢失但构建照样"成功"。
#    让 CI 自行解析，才能拿到 3.1.0 + sass-embedded（Dart Sass）。
.bundle
vendor
Gemfile.lock

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/vendors
assets/js/dist

# Hidden system files
*~
.DS_Store
Thumbs.db

# Secrets / private keys —— 绝不可提交
# （曾误将 SSH 私钥 y / y.pub 提交进公开历史，教训深刻）
*.pem
*.key
*.p12
*.pfx
*.keystore
*.jks
id_rsa
id_ed25519
id_dsa
id_ecdsa
id_ecdsa_sk
id_ed25519_sk
y
y.pub
.env
.env.*
*.secret
credentials
credentials.*
